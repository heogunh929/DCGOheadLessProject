# DCGO Full Card Pool Progress

| Date | Queue | Result | Baseline commit | Tests | Blocker/Next |
| --- | --- | --- | --- | --- | --- |
| 2026-06-20 | start | 최신 source 검수 후 전체 카드풀 목표 queue 생성 | `535163ad` | GitHub CI 없음; 커밋 소스에 runtime composition tests 추가 확인 | 52 dependency injection부터 진행 |
| 2026-06-20 | 52 | `RuleProcessor`가 linked overflow 정리 시 injected `IZoneMover`를 사용하도록 정렬하고, `BattleEngineServices` graph validation에 `RuleProcessor` zone mover 공유 검사를 추가했다. 공통 layer source mapping 문서도 갱신했다. | `535163ad` | 로컬 `.\.dotnet\dotnet.exe run --no-restore --project .\src\DCGO.RL.Engine.Tests\DCGO.RL.Engine.Tests.csproj` -> `All 248 tests passed.` MSBuild temp/cache access warning 있음 | 다음 항목: 52A 공통 pending decision pause/resume boundary |
| 2026-06-20 | 52A | `EngineSession.Step/Resume`, `EngineStepResult`, `DecisionResult`, `TriggerPipelineContinuation` 기반 resume을 추가해 hand option `OptionSkill`, chained option selection, normal play `OnPlay`, normal digivolve `WhenDigivolving`, `OnAllyAttack`, `OnEndAttack`, attack security check 중 `SecuritySkill`/Activate Main selection, rules timing, `OnStartMainPhase`, `PassAction`의 `OnEndTurn`/`OnStartTurn` pending selection을 공통 boundary에서 처리한다. action/selection/phase trace와 `ReplayRunner(services: ...)` replay를 추가했고, services 기반 `ScriptedScenarioRunner`/`RandomLegalActionRunner`는 pending selection을 `ScenarioRunStatus.PausedForDecision`로 반환한다. | `535163ad` + local 52 changes | 로컬 `.\.dotnet\dotnet.exe run --no-restore --project .\src\DCGO.RL.Engine.Tests\DCGO.RL.Engine.Tests.csproj` -> `All 266 tests passed.` | needs-review: full security timing sequence는 queue 53에서 source-aligned 순서로 정렬 필요 |
